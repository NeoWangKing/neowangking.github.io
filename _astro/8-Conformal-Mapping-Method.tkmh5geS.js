const n="mathematics/MMiP/8-Conformal-Mapping-Method.md",$="posts",a="mathematics/mmip/8-conformal-mapping-method",t=`
## 引言

许多物理问题（如静电场、热传导、无旋流体）在二维情况下归结为求解拉普拉斯方程 $\\nabla^2 u = 0$ 在复杂区域内的边值问题。保角变换（共形映射）利用解析函数的几何性质，将复杂区域映射为简单区域（如半平面、圆），在简单区域上求解后再变回原区域，从而得到原问题的解。这一方法的物理基础是：二维拉普拉斯方程在保角变换下形式不变。

## 基本概念与定理

### 保角变换的定义

若函数 $w = f(z) = u(x,y) + \\mathrm{i}v(x,y)$ 在区域 $D$ 内解析，且 $f'(z) \\neq 0$，则 $f$ 在 $D$ 内是**保角的**：两条光滑曲线的交角在变换下保持大小和方向不变。因此解析函数实现的映射也称为**共形映射**。

### 拉普拉斯方程的不变性

设 $\\varphi(u,v)$ 是 $w$ 平面上的调和函数（满足 $\\varphi_{uu} + \\varphi_{vv} = 0$），在保角变换 $w = f(z)$ 下，复合函数 $\\Phi(x,y) = \\varphi\\big( u(x,y), v(x,y) \\big)$ 在 $z$ 平面原区域上仍然是调和函数。这为解题提供了依据：可以将 $xy$ 平面上的复杂边界问题变换到 $uv$ 平面上求解，再映射回原坐标。

### 边值问题的变换原则

对于第一类边界条件（给定函数值），在像平面上直接指定对应边界上的值即可；对于第二类边界条件（给定法向导数），由于保角变换会改变线段长度，**法向导数的变换须考虑尺缩因子**，通常在应用中只处理第一类边界条件或通过复位势函数等方法。

## 常用初等映射

### 线性函数

$$
\\begin{align}
w = z + b, \\quad w = az \\ (a \\neq 0)
\\end{align}
$$

实现平移、旋转和缩放。不改变区域形状，仅作刚体变换。

### 幂函数

$$
\\begin{align}
w = z^\\alpha, \\quad \\alpha > 0
\\end{align}
$$

将角形区域 $\\{ z : 0 < \\arg z < \\theta \\}$ 映射为角形区域 $\\{ w : 0 < \\arg w < \\alpha\\theta \\}$。特别地，$\\alpha = \\pi/\\theta$ 可将角域展成上半平面。例如 $w = z^2$ 将第一象限映射到上半平面。

### 指数函数与对数函数

指数函数 $w = e^z$ 将水平带域 $0 < \\operatorname{Im}(z) < \\pi$ 映射为上半平面 $\\operatorname{Im}(w) > 0$；将垂直线段映射为圆弧。对数函数 $w = \\ln z$ 常用来将角域或环形域化为带域。

### 分式线性变换（莫比乌斯变换）

$$
\\begin{align}
w = \\frac{az + b}{cz + d}, \\quad ad - bc \\neq 0
\\end{align}
$$

分式线性变换将圆和直线仍然映射为圆或直线（广义圆）。它有三个自由度，可通过指定三对对应点唯一确定映射。常用此变换将半平面映射为单位圆，或将偏心圆环变为同心圆环。

### 施瓦茨-克里斯托费尔映射

将上半平面（或单位圆）内部映射到多边形的内部，其导数为

$$
\\begin{align}
\\frac{\\mathrm{d}w}{\\mathrm{d}z} = A \\prod_{k=1}^n (z - x_k)^{\\alpha_k/\\pi - 1}
\\end{align}
$$

其中 $x_k$ 是实轴上的点（原像顶点），$\\alpha_k$ 是多边形对应内角。该映射是处理多边形边界的利器。

## 典型应用步骤

1. **分析问题**：写出原问题（$z$ 平面）的方程 $\\nabla^2 u = 0$ 及边界条件。
2. **选择映射**：根据原区域形状，选取适当的解析函数 $w = f(z)$，将区域映射为简单标准区域（上半平面、单位圆、带域等）。
3. **转化边值**：将原边界条件转移到 $w$ 平面上相应边界处。
4. **求解简单问题**：在新平面上求解拉普拉斯方程的边值问题（常用分离变量法、镜像法或直接积分）。
5. **返回原变量**：用逆映射 $z = f^{-1}(w)$ 将解写为 $x,y$ 的函数。

### 例：半无限板上的稳态温度分布

一块半无限大平板 $x \\ge 0,\\ -\\infty < y < \\infty$，在边界 $x=0$ 上有一段 $-a < y < a$ 保持温度 $T_0$，其余边界绝热。求板内稳态温度分布。

**解**：稳态温度满足 $\\nabla^2 T = 0$。区域为右半平面减去一条绝热裂缝，较复杂。但可通过保角变换简化。考虑映射 $w = \\arcsin(z/a)$，它把 $z$ 平面的割缝区域映射到 $w$ 平面上的一个带域，边界条件化为已知。然后在带域内求解拉普拉斯方程，再由逆变换给出原问题的解。详细过程略，此处仅示范步骤。

### 例：共面电极的静电场

两无限长共面半无限导体板分别保持电位 $V_1$ 和 $V_2$，缝隙很小。利用施瓦茨-克里斯托费尔映射可将该退化多边形映射为上半平面，进而求得电位分布。

## 注意事项

- 保角变换法主要适用于**二维调和函数**的边值问题。
- 映射通常将原区域边界一一对应到新区域边界，因此原边界条件可逐点转移。
- 对于第二类边界条件，须补充尺缩因子 $|f'(z)|$ 的修正，计算稍繁。
- 许多工程问题可借助已知映射表格或数值保角映射求解。

## 总结

- 二维拉普拉斯方程在保角变换下形式不变，这是保角变换法的理论基础。
- 通过适当的解析映射，复杂边界问题可化为简单标准区域上的问题。
- 常用的初等映射（线性、幂、指数、分式线性）及其组合覆盖了大量典型边界形状。
- 保角变换法在静电学、流体力学、热传导等二维问题中有广泛应用。
`,e={title:"保角变换法",author:"NeoWangKing",date:new Date(17779632e5),lastMod:new Date(17779632e5),tags:["数学","数学物理方法","保角变换","共形映射","拉普拉斯方程","边值问题"],category:["数学","数学物理方法"],summary:"本文介绍利用复变函数中的保角变换（共形映射）求解二维拉普拉斯方程的边值问题，包括基本定理、常用初等映射、施瓦茨-克里斯托费尔映射，以及典型应用实例。",comments:!0,draft:!1,sticky:0},i={type:"content",filePath:"/home/runner/work/blog-source-code/blog-source-code/src/content/posts/mathematics/MMiP/8-Conformal-Mapping-Method.md",rawData:void 0};export{i as _internal,t as body,$ as collection,e as data,n as id,a as slug};
